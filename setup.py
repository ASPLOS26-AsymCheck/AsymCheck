from setuptools import setup, find_packages

setup(name='delaycheck_lib',
      version='1.0',
      description='Reconstructing the DeepSpeed training framework',
      packages=find_packages()
    )
